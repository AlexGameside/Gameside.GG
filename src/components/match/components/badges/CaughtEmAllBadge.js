import Badge from "./Badge";
import CaughtEmAll from "../../../../svgs/CaughtEmAll";

const CaughtEmAllBadge = () => {
  return (
    <Badge label="Caught 'Em All" color={"#transparent"}>
      <CaughtEmAll size={62} />
    </Badge>
  );
};

export default CaughtEmAllBadge;
